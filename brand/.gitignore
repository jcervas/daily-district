# Regenerated on every run of the exploration scripts — only the numbered contact
# sheets in explorations/ are worth keeping.
explorations/explore*/
explorations/compare/
# Scratch previews written while checking renders.
dist/px/
__pycache__/
.ico/
# The design handoff the district mark was built from — the zip as delivered and the
# same files unpacked. Kept out of the repo because the site is served straight from
# it; the original lives in Drive. build.py is the source of truth for the geometry.
explorations/*.zip
explorations/district-mark-handoff/
