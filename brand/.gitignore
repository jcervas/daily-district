# Regenerated on every run of the exploration scripts — only the numbered contact
# sheets in explorations/ are worth keeping.
explorations/explore*/
explorations/compare/
# Scratch previews written while checking renders.
dist/px/
__pycache__/
.ico/
# The design handoff as delivered. The unpacked copy in district-mark-handoff/
# is the one to read, and this is a 51KB binary the static site would otherwise serve.
explorations/*.zip
